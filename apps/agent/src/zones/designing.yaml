zone:
  # ─── 身份(必填 · 5 字段) ───
  id: designing
  name: DESIGNING
  display_name: 设计
  icon: 🎨
  route_segment: designing

  # ─── 环境(必填 · 5 字段) ───
  has_resource_tree: false
  has_inline_rail: false
  main_layout: proposal-review
  status_color: yellow
  status_pulse: false

  # ─── 装备(必填 · 1 字段) ───
  default_arming:
    - proposal-evaluator
    - trade-off-analysis

  # ─── 触发器(可选 · 2 字段) ───
  entry_triggers: []
  exit_triggers: []

  # ─── 备注(可选 · 1 字段) ───
  description: 评审 AI 候选方案,选择最终路径