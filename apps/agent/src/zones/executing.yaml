zone:
  # ─── 身份(必填 · 5 字段) ───
  id: executing
  name: EXECUTING
  display_name: 执行中
  icon: ⚡
  route_segment: executing

  # ─── 环境(必填 · 5 字段) ───
  has_resource_tree: true
  has_inline_rail: true
  main_layout: mission-control
  status_color: green
  status_pulse: false

  # ─── 装备(必填 · 1 字段) ───
  default_arming:
    - code-scaffold
    - code-review
    - test-gen
    - commit-message-draft

  # ─── 触发器(可选 · 2 字段) ───
  entry_triggers: []
  exit_triggers: []

  # ─── 备注(可选 · 1 字段) ───
  description: 监督 AI 实施,审阅 PR 与测试