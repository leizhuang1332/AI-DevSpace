zone:
  # ─── 身份(必填 · 5 字段) ───
  id: analyzing
  name: ANALYZING
  display_name: 分析中
  icon: 🔍
  route_segment: analyzing

  # ─── 环境(必填 · 5 字段) ───
  has_resource_tree: false
  has_inline_rail: false
  main_layout: observer
  status_color: blue
  status_pulse: true

  # ─── 装备(必填 · 1 字段) ───
  default_arming:
    - codebase-context
    - dependency-graph

  # ─── AI 思考条(必填 · 1 字段) ───
  thinking_bar: required

  # ─── 触发器(可选 · 2 字段) ───
  entry_triggers: []
  exit_triggers: []

  # ─── 备注(可选 · 1 字段) ───
  description: AI 旁观思考,展示推理过程